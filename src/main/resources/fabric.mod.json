{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Experimental mod to sync the ingame chat with a WhatsApp group chat.",
  "authors": [
    "WALink"
  ],
  "license": "MIT",
  "icon": "assets/walink/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
        "net.justacoder.walink.WALMain"
    ]
  },
  "mixins": [
    "walink.mixins.json"
  ],
  "accessWidener": "walink.accesswidener",
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}",
	"fabric-api": "*",
    "java": ">=21"
  }
}